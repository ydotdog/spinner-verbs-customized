# Spinner Verbs Customized

自定义的 Claude Code CLI spinner 动词集合。

## 说明

替换 Claude Code CLI 默认的加载动画文字，显示个性化的 spinner 短语。涵盖投资、AI、哲学、创业等多个领域的趣味文案。

## 文件

- `spinner-verbs-default.json` — 默认动词集
- `spinner-verbs-kyle.json` — 自定义动词集

## 使用方式

将 JSON 文件内容配置到 Claude Code 的 spinner verbs 设置中即可生效。
